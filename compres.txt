for d in */; do
    tar -I zstd -cf "${d%/}.tar.zst" "$d"
done

tar -I zstd -cf archive.tar.zst archive

tar -I zstd -xf archive.tar.zst


tar --exclude='workspaces-java' -czpf nixos-config.tar.gz nixos-config

tar -xzpf project-backup.tar.gz
